import React from 'react'

const TestComponent = ({count, person}) => {
  return (
    <>
        {/* <h1>TestComponent</h1>
        <h3>{count}</h3>
        <p>{person.name}</p>
        <p>{person.age}</p>
        <TestComponent2 count={count} person={person}/> */}
    </>
  )
}

export default TestComponent